# {{ ansible_managed }}

description "Archivematica Dashboard"
author "Artefactual Systems Inc."

start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

exec {{ archivematica_src_am_dashboard_env_dir }}/bin/gunicorn --config /etc/archivematica/dashboard.gunicorn-config.py wsgi:application
